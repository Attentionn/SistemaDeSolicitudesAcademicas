# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sistema_solicitudes
DB_USER=root
DB_PASSWORD=

# JWT Secret (generar uno nuevo con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=tu_secreto_jwt_aqui_cambiar

# Server
PORT=5000
NODE_ENV=development